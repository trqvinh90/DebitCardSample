import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    rowContainer: {
        marginTop: 15,
        flexDirection: 'row',
        alignItems: 'center',
    },
    balanceHeader: {
        color: '#FFFFFF',
        fontSize: 14,
        marginTop: 24,
    },
});

export default styles;