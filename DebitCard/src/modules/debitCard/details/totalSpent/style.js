import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    rowContainer: {
        flexDirection: 'row',
        alignContent: 'center',
        marginBottom: 6,
    },
    text: {
        fontSize: 13,
        color: '#222222',
    },
});

export default styles;